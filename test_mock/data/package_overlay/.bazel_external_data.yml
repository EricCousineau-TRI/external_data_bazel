package:
    remote: test_package_overlay

    remotes:
        test_package_overlay:
            overlay: ..
            backend: mock
            dir: mock/backend_child
            upload_dir: /tmp/bazel_external_data/upload
